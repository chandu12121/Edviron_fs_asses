Develop a backend service for a school payment and dashboard application. The focus of this assessment is to createa REST API for managing students, fees, and payment transactions.
Initialize a Node.js project with Express.js and connect it to MongoDB.
Develop a responsive and user-friendly interface for the School Payments and Dashboard application. The focus is on creating a React-based frontend that integrates with the backend APIs to display and manage data dynamically.
Set up a new project using Create React App.
Style the application with CSS
Use Axios for API calls and React Router for navigation.
Transaction Status Check
dedicated page or modal where users can input trasaction_id and retrieve the current status of a transaction using the check-status API.
Dark Mode: Implement a toggle for light/dark themes.
Include a filter dropdown for status ("Success", "Pending", "Failed").
Include a filter for date-wise transactions (by selecting a date range).
